<%= form_with(model: category, url: admin_categories_path, method: :post) do |form| %>
  <!-- Add your category form fields here -->
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :description %>
  <%= form.text_area :description %>

  <%= form.submit 'Create Category' %>
<% end %>